#include "SeeOtherAgent.h"

bool SeeOtherAgent::IsTrue(Agent* agent)//returns true if agent can see another agent
{
	//needs a condition on checking to see if we can see other agents
	//Didn't finish, this one along with attack.cpp
	return LineOfSight;
}
